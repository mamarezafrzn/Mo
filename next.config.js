/** @type {import('next').NextConfig} 


// module.exports = {

// }
/** @type {import('next').NextConfig} */
const nextConfig = {
    // ❌ REMOVE if present – static export disables API routes
    // output: 'export',
  
    // ❌ REMOVE or set to true – disabling appDir blocks app/ routes
    // experimental: { appDir: false },
  
    // If you use basePath, remember it in the URL:
    // basePath: '/portfolio',
  };
  module.exports = nextConfig;